using UnityEngine;

namespace Assets.scripts
{
    public class PerSecComponent : MonoBehaviour {

        // Use this for initialization
        void Start () {
	
        }
	
        // Update is called once per frame
        void Update () {
	
        }
    }
}
